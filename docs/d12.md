# DELI 12 - Full Win32 Integration 

## Objective
- finish the `UI -> input -> Chloe -> proc -> output -> UI -> Display` pipeline
- make chloe an actual library, no more using stdout and stdin

## Learning
- Memory ownership and passing pipeline design, UI owns the buffer memory as it is what loops and calls chloe 
- made a struct to keep track of pointer end while writing into the memory
- `SetCurrentDirectoryW` working (it only affects the process its called from)
- Importancee of initialization of memory (othwis its just random mem that can just cease to exist)
- UI + Library integration 

## Outcome
fully transformed and integrated Chloe into Win32 UI, yippiee