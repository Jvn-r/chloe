# DELI 3 — `echo` and `pwd` and `cd` functions

## Objective
perform `echo` and `pwd` and `cd` commads as in-built commands

## Learning
- Aware of word spacing handling, to not leave any extra spaces
- `GetCurrentDirectory` specialized returns of returning size of the directory when given 0 and Null as inputs
- memory allocation refresh
- `SetCurrentDirectory` usage, and handling empty path similar to WIndows cmd

## Outcome
`echo` and `pwd` and `cd` work as in-built commands
