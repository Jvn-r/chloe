# DELI 3 — Revamped Tokenizer

## Objective
Scrap the old `strtok` based destructive tokenizer and make a new one that will work with the upcoming features like chaining and pipes

## Learning
- differences in `char` and `string` behaviours
- revisiting structs and lookup tables ish pattern
- realize shortcomings of `strtok`
- `typedef enum` usage 
- global variable behaviours

## Outcome
We have a lexical char by char rule based tokenizer
